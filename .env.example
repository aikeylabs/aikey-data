# aikey-data local development environment
# Copy to .env and adjust as needed.

# Service authentication token (shared by collector-service and query-service)
SERVICE_TOKEN=dev-token

# Log level: debug, info, warn, error
LOG_LEVEL=info

# PostgreSQL port mapping (change if 5432 is already used by aikey-control)
# PG_PORT=5433
